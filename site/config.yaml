baseURL: "https://example.com/"
languageCode: "zh-TW"
title: "從 AI 輔助到規格驅動 (SDD) 實戰攻略"

params:
  description: "Course materials, assignments, and resources."
  site_author: "Course Team"

menu:
  main:
    - name: "課程章節"
      url: "/lessons/"
      weight: 10
    - name: "相關資源"
      url: "/resources/"
      weight: 30
  sidebar:
    - name: "相關資源"
      url: "/resources/"
      weight: 10

markup:
  goldmark:
    renderer:
      # Required to allow render hooks (e.g., plantuml) to output raw HTML/SVG
      unsafe: true

module:
  imports:
    - path: github.com/imfing/hextra
